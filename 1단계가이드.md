# 마음 우체통 — 실행과 배포 안내

익명 사랑 고백 게시판입니다. 로그인이 없고, 글마다 익명 별명이 저절로 붙습니다.

---

## 1. 내 컴퓨터에서 열어 보기

검은 창(명령 프롬프트)에서 이 폴더로 간 다음, 아래를 차례로 칩니다.

```
npm install
npm run build
node 로컬서버.mjs
```

그리고 브라우저 주소창에 이렇게 칩니다.

```
http://localhost:5180
```

끄려면 그 검은 창에서 **Ctrl + C** 를 누릅니다.

> 이때는 글이 내 컴퓨터의 임시 파일에 저장됩니다.
> 인터넷에 올리면 아래 3번의 진짜 저장소를 씁니다.

---

## 2. Vercel 에 올리기

### 2-1. 준비물

- GitHub 계정 (무료)
- Vercel 계정 (무료, GitHub 로 로그인하면 편합니다)

### 2-2. GitHub 에 코드 올리기

이 폴더에서:

```
git init
git add .
git commit -m "마음 우체통 첫 버전"
```

GitHub 에서 새 저장소(repository)를 하나 만든 뒤, 화면에 나오는
`git remote add ...` 와 `git push ...` 두 줄을 그대로 복사해 붙여 넣습니다.

### 2-3. Vercel 에서 가져오기

1. https://vercel.com 접속 → **Add New** → **Project**
2. 방금 만든 GitHub 저장소를 고릅니다
3. 설정은 **손대지 않아도 됩니다.** (`vercel.json` 에 다 적혀 있습니다)
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
4. **Deploy** 를 누릅니다

여기까지 하면 화면은 뜨지만, **글을 쓰면 오류가 납니다.**
저장할 곳을 아직 안 붙였기 때문입니다. 3번으로 갑니다.

---

## 3. ★ 저장소 붙이기 (이걸 안 하면 글이 안 써집니다)

1. Vercel 에서 방금 만든 프로젝트를 엽니다
2. 위쪽 **Storage** 탭 → **Create Database**
3. **Upstash** 의 **Redis** 를 고릅니다 (무료 요금제 선택)
4. 이름은 아무거나 (예: `maeum-db`) → 만들기
5. 만들어진 저장소에서 **Connect Project** 를 눌러 이 프로젝트에 연결합니다

연결하면 Vercel 이 아래 두 값을 **자동으로** 넣어 줍니다.
직접 타이핑할 필요가 없습니다.

```
KV_REST_API_URL
KV_REST_API_TOKEN
```

6. 마지막으로 **Deployments** 탭 → 맨 위 배포의 **⋯** → **Redeploy**

이제 글쓰기·하트·댓글이 전부 됩니다.

> 잘 붙었는지 확인하려면 브라우저에서 이렇게 열어 보세요.
> `https://내주소.vercel.app/api/board?do=ping`
> `"저장소":"Upstash Redis"` 라고 나오면 성공입니다.
> `"파일(...)"` 이라고 나오면 아직 연결이 안 된 것입니다.

---

## 4. 바탕화면에 아이콘 넣기 (웹앱 설치)

배포한 주소를 열면 첫 화면에 **"바탕화면에 아이콘 넣기"** 안내가 뜹니다.

| 기기 | 방법 |
| --- | --- |
| 안드로이드 · PC 크롬/엣지 | 안내의 **⬇️ 지금 설치하기** 를 누르면 끝 |
| 아이폰 (사파리) | 아래 **공유 단추(□↑)** → **홈 화면에 추가** → **추가** |

- 설치하면 주소창 없이 앱처럼 열립니다.
- 이미 설치했거나 앱으로 열었으면 안내가 저절로 사라집니다.
- 안내 오른쪽 **×** 를 누르면 다시 안 보입니다.

> ⚠️ 설치 단추는 **https 주소에서만** 나옵니다.
> `http://localhost` 는 예외로 되지만, 회사망 등 `http://` 주소에서는 안 뜹니다.

---

## 5. 이 게시판의 규칙

- **이름을 묻지 않습니다.** 글마다 `조용한 민들레 #3A7F` 같은 별명이 자동으로 붙습니다.
- **지울 때 쓸 숫자 4자리**를 정해 둡니다. 이걸 잊으면 글을 못 지웁니다.
- 하트와 신고는 **한 사람이 한 번**만 됩니다.
- **신고가 3번** 쌓이면 그 글은 자동으로 가려집니다.
- 같은 사람이 **20초 안에** 연달아 쓰지 못합니다. (도배 방지)
- 글은 600자, 받는 사람은 20자, 댓글은 200자까지입니다.

---

## 6. 스스로 점검하기

```
node 점검.mjs --직접      서버 기능 34가지 (글쓰기~지우기~신고)
node 화면점검.mjs         진짜 크롬으로 화면 29가지 (설치 준비물 포함)
```

`화면점검.mjs` 는 먼저 `node 로컬서버.mjs` 를 켜 두어야 합니다.

---

## 7. 폴더 안내

```
api/board.js       서버 (글쓰기·하트·댓글·신고·지우기)
api/_저장소.js      저장 담당 (Vercel=Upstash / 내 컴퓨터=파일)
src/App.jsx        첫 화면 (목록·검색·태그)
src/components/    글쓰기·글상세·설치안내 창
public/icons/      앱 아이콘
public/sw.js       설치와 빠른 실행을 돕는 일꾼
로컬서버.mjs        내 컴퓨터에서 Vercel 흉내 내기
vercel.json        Vercel 배포 설정
```
